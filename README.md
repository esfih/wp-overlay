# wp-overlay

Reusable WordPress-specific development overlay for app-local product repositories.

This repository owns:

- WordPress runtime scaffolding
- plugin packaging helpers
- WordPress-specific validation scripts
- reusable plugin-template and Docker assets

App-local repos should import this repository into `foundation/wp` via subtree.